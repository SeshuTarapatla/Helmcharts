# Helmcharts
All helmcharts related to my Kubernetes deployments in one place.

> Use `cmd` for helm installs when dealing with senstive args.

## Postgres

To install Postgres helmchart:

```bash
helm install postgres .\Postgres\ --set password=***
```

