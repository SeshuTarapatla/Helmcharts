apiVersion: v1
kind: Secret
metadata:
  name: {{ .Values.app }}-secret
type: Opaque
stringData:
  POSTGRES_USER: {{ .Values.user }}
  POSTGRES_PASSWORD: {{ .Values.password | quote }}
  POSTGRES_PORT: {{ .Values.port | quote }}