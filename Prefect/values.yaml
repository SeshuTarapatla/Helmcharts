app: "prefect"
image: "prefect-image:latest"
server:
  port: 4200
  workpool: default-pool
postgres:
  configmap: "postgres-configmap"
  secret: "postgres-auth-secret"
  service: "postgres-service"
  image: "postgres:alpine"